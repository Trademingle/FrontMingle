import React from 'react';
import Messenger from './components/Messenger';

export default function InboxPage() {
    return (
      <React.Fragment>
        <Messenger />
      </React.Fragment>
    );
}